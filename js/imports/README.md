# imports/

Code d'import manuel existant, extrait tel quel : base.js (utilitaires communs, ptKey), grafana.js (arrets Inpak + bulk, coller-dans-textarea), protime.js (pointages + absences), ncp.js.

Regle du plan Phase 2 : uniquement le code d'import MANUEL existant. Aucune automatisation, aucune API, aucun cron, aucun token cette phase -- l'automatisation Grafana/Protime est Phase 3.

Cree a l'etape 1 (dossier vide, aucun code deplace pour l'instant).
