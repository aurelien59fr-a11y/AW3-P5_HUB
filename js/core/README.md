# core/

Modules transverses sans logique metier : firebase.js (acces DB), auth.js (canEdit), ui.js (toast, escHtml), format.js (dates, equipes, getEquipe/getOperateur).

Cree a l'etape 1 du plan Phase 2 (dossiers vides, aucun code deplace pour l'instant).
